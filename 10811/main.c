/*
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
    int n,a[100];
    a[0] = 0;
    a[1] = 1;
    scanf("%d", &n);
    for (int i = 2; i <= n; i++) {
        a[i] = a[i - 1] + a[i - 2];
    }
    printf("%d", a[n]);
}
*/


